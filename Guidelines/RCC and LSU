
🔹 1. Render content conditionally (рендерить контент условно)
Это значит: в методе build() ты возвращаешь разные виджеты в зависимости от состояния.
Это локальное решение внутри одного build().
Ты смотришь на состояние (переменную в State) и решаешь: какой именно виджет вернуть.
Применяется, когда состояние нужно только этому виджету.


🔹 2. Lifting state up (подъём состояния вверх)
Это важный паттерн: состояние хранится выше в иерархии, а “дети” получают его через параметры.
Почему? Потому что несколько виджетов могут нуждаться в одном и том же состоянии.

состояние “поднято” в родителя, чтобы оно было доступно и управляло рендерингом детей.

Здесь уже не один виджет решает сам за себя.
Ты “поднимаешь” состояние выше в дереве (в родителя), чтобы оно было доступно нескольким детям.
Родитель управляет состоянием, а дети просто:
либо отображают часть состояния (получают данные через параметры),
либо вызывают колбэки, чтобы “попросить” родителя изменить состояние.



