
🔹 Ключевая разница
final List<String> answers; с обычным списком:
саму ссылку нельзя переприсвоить,
но можно менять содержимое (add, remove).
final List<String> answers; с const списком:
нельзя ни переприсвоить,
ни изменить содержимое (add, remove → ❌ ошибка).

